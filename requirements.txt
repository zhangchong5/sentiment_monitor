# ============================================
# 核心依赖
# ============================================
requests>=2.31.0
beautifulsoup4>=4.12.0
readability-lxml>=0.8.1
pydantic>=2.5.0
python-dotenv>=1.0.0
jinja2>=3.1.0

# ============================================
# PDF 生成（Playwright）
# ============================================
# Playwright 同时用于截图和 PDF 生成，无需 WeasyPrint
playwright>=1.40.0
# 首次运行需安装浏览器：playwright install chromium

# ============================================
# 其他工具
# ============================================
loguru>=0.7.0  # 可选：增强日志（当前使用标准 logging）
